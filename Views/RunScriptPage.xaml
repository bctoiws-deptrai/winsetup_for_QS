<UserControl x:Class="BctWinsetup.Views.RunScriptPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="600">
    <UserControl.Resources>
        <Style TargetType="Border" x:Key="ScriptCardStyle">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D2D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
        <Style TargetType="Button" x:Key="RunButtonStyle">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#219653"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#2D2D2D"/>
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4D4D4D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Chạy Script Tự Động" Foreground="#FFFFFF" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI"/>
            <TextBlock Text="Thực thi các script cấu hình hoặc tối ưu hóa hệ thống bằng PowerShell hoặc Command Prompt dưới quyền Admin." Foreground="#A0A0A0" FontSize="12.5" Margin="0,4,0,0" FontFamily="Segoe UI"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource ScriptCardStyle}" Padding="12" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Môi trường chạy:" Foreground="#A0A0A0" FontSize="12" VerticalAlignment="Center" Margin="0,0,12,0" FontFamily="Segoe UI"/>
                            <RadioButton x:Name="RadPowerShell" Content="PowerShell" Foreground="#FFFFFF" FontSize="12" FontFamily="Segoe UI" IsChecked="True" VerticalAlignment="Center" Margin="0,0,16,0"/>
                            <RadioButton x:Name="RadCmd" Content="Command Prompt (CMD)" Foreground="#FFFFFF" FontSize="12" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="BtnSaveDefault" Content="Lưu script mặc định" Style="{StaticResource ActionButtonStyle}" Click="BtnSaveDefault_Click" Margin="0,0,8,0"/>
                            <Button x:Name="BtnLoadDefault" Content="Tải script mặc định" Style="{StaticResource ActionButtonStyle}" Click="BtnLoadDefault_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <TextBox x:Name="TxtScript" Grid.Row="1" Background="#181818" Foreground="#FFFFFF" BorderBrush="#2D2D2D" BorderThickness="1" FontFamily="Consolas" FontSize="12" AcceptsReturn="True" AcceptsTab="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,0,0,12" Padding="8"/>

                <Button x:Name="BtnRunScript" Grid.Row="2" Content="BẮT ĐẦU CHẠY SCRIPT (ADMIN)" Style="{StaticResource RunButtonStyle}" Click="BtnRunScript_Click"/>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Kết quả thực thi (Console Output):" Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,6" FontFamily="Segoe UI"/>
                <TextBox x:Name="TxtLog" Grid.Row="1" Background="#0C0C0C" Foreground="#00FF00" BorderBrush="#2D2D2D" BorderThickness="1" FontFamily="Consolas" FontSize="11" IsReadOnly="True" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" TextWrapping="Wrap"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <TextBlock x:Name="TxtStatus" Text="Sẵn sàng" Foreground="#CCCCCC" FontSize="12" FontFamily="Segoe UI" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TxtPercentage" Text="0%" Foreground="#CCCCCC" FontSize="12" FontFamily="Segoe UI" HorizontalAlignment="Right" Visibility="Collapsed"/>
            </Grid>
            <ProgressBar x:Name="ProgBar" Grid.Row="1" Height="4" Background="#1E1E1E" Foreground="#27AE60" Minimum="0" Maximum="100" BorderThickness="0" IsIndeterminate="False"/>
        </Grid>
    </Grid>
</UserControl>
