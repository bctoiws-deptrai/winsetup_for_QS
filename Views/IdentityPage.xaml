<UserControl x:Class="BctWinsetup.Views.IdentityPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="600">
    <UserControl.Resources>
        
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D2D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0086F0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Định Danh Thiết Bị &amp; Người Dùng" 
                       Foreground="#FFFFFF" 
                       FontSize="22" 
                       FontWeight="SemiBold" 
                       FontFamily="Segoe UI"/>
            <TextBlock Text="Cá nhân hóa tên máy tính và tài khoản đăng nhập hệ thống sau khi cài đặt lại Windows." 
                       Foreground="#A0A0A0" 
                       FontSize="13" 
                       Margin="0,4,0,0" 
                       FontFamily="Segoe UI"/>
        </StackPanel>

        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                
                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        
                        <Border Grid.Column="0" Width="48" Height="48" CornerRadius="24" Background="#1D2E3C" Margin="0,0,20,0" VerticalAlignment="Top">
                            
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Rectangle Width="20" Height="14" Stroke="#00A2ED" StrokeThickness="2" RadiusX="1" RadiusY="1"/>
                                <Path Data="M 6,14 L 14,14 L 10,18 Z" Fill="#00A2ED" Margin="0,4,0,0"/>
                            </Grid>
                        </Border>
                        
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Thay Đổi Tên Máy Tính (Computer Name)" Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" FontFamily="Segoe UI"/>
                            
                            <Grid Margin="0,10,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Tên hiện tại: " Foreground="#888888" FontSize="13" FontFamily="Segoe UI"/>
                                <TextBlock Grid.Column="1" x:Name="TxtCurrentPCName" Text="Loading..." Foreground="#00A2ED" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI"/>
                            </Grid>

                            <TextBlock Text="Đổi tên thiết bị giúp bạn dễ dàng nhận diện máy tính trong mạng nội bộ hoặc khi chia sẻ tệp tin." 
                                       Foreground="#888888" FontSize="12.5" TextWrapping="Wrap" Margin="0,0,0,16" FontFamily="Segoe UI"/>
                            
                            <Button Content="Mở Cửa Sổ Đổi Tên Máy (sysdm.cpl)" 
                                    Style="{StaticResource ActionButtonStyle}" 
                                    HorizontalAlignment="Left"
                                    Click="BtnRenamePC_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                
                <Border Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        
                        <Border Grid.Column="0" Width="48" Height="48" CornerRadius="24" Background="#1A2D24" Margin="0,0,20,0" VerticalAlignment="Top">
                            
                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Ellipse Width="10" Height="10" Stroke="#2ECC71" StrokeThickness="2" Margin="0,0,0,12"/>
                                <Path Data="M 2,16 C 2,10 18,10 18,16" Stroke="#2ECC71" StrokeThickness="2" Fill="Transparent" Margin="0,8,0,0"/>
                            </Grid>
                        </Border>
                        
                        
                        <StackPanel Grid.Column="1" Margin="20,0,0,0">
                            <TextBlock Text="Thay Đổi Tên Tài Khoản (User Account)" Foreground="#FFFFFF" FontSize="15" FontWeight="SemiBold" FontFamily="Segoe UI"/>
                            
                            <Grid Margin="0,10,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Tài khoản hiện tại: " Foreground="#888888" FontSize="13" FontFamily="Segoe UI"/>
                                <TextBlock Grid.Column="1" x:Name="TxtCurrentUserName" Text="Loading..." Foreground="#2ECC71" FontSize="13" FontWeight="Bold" FontFamily="Segoe UI"/>
                            </Grid>

                            <TextBlock Text="Đổi tên đăng nhập/tên hiển thị của tài khoản cục bộ hiển thị trên màn hình khóa Windows." 
                                       Foreground="#888888" FontSize="12.5" TextWrapping="Wrap" Margin="0,0,0,16" FontFamily="Segoe UI"/>
                            
                            <Button Content="Mở Cửa Sổ Đổi Tên User (netplwiz)" 
                                    Style="{StaticResource ActionButtonStyle}" 
                                    HorizontalAlignment="Left"
                                    Click="BtnRenameUser_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                
                <Border Background="#251F15" BorderBrush="#6E562F" BorderThickness="1" CornerRadius="8" Padding="16">
                    <StackPanel>
                        <TextBlock Text="LƯU Ý QUAN TRỌNG:" Foreground="#F39C12" FontSize="13" FontWeight="Bold" Margin="0,0,0,6" FontFamily="Segoe UI"/>
                        <TextBlock Text="1. Việc đổi tên máy tính yêu cầu bạn phải khởi động lại (Restart) thiết bị để có hiệu lực." 
                                   Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,4" FontFamily="Segoe UI"/>
                        <TextBlock Text="2. Khi cửa sổ netplwiz mở ra, hãy chọn tài khoản của bạn -> click Properties -> nhập tên mới vào mục User Name." 
                                   Foreground="#A0A0A0" FontSize="12" FontFamily="Segoe UI"/>
                    </StackPanel>
                </Border>
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
