<UserControl x:Class="BctWinsetup.Views.BeforeWinBackupPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="600">
    <UserControl.Resources>
        <Style TargetType="Border" x:Key="SectionCardStyle">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="BorderBrush" Value="#2D2D2D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#2D2D2D"/>
                                <Setter Property="Foreground" Value="#555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button" x:Key="SecondaryButtonStyle">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3D3D3D"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4D4D4D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#1E1E1E"/>
                                <Setter Property="Foreground" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Sao Lưu Dữ Liệu Trước Khi Cài Win" Foreground="#FFFFFF" FontSize="20" FontWeight="SemiBold" FontFamily="Segoe UI"/>
            <TextBlock Text="Đảm bảo an toàn cho dữ liệu Desktop và ghi nhớ các ứng dụng đã cài đặt trên máy." Foreground="#A0A0A0" FontSize="12.5" Margin="0,4,0,0" FontFamily="Segoe UI"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource SectionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="1. SAO LƯU DỮ LIỆU DESKTOP" Foreground="#00A2ED" FontSize="14" FontWeight="Bold" Margin="0,0,0,12" FontFamily="Segoe UI"/>
                            
                            <TextBlock Text="Thư mục sao lưu đích (Khuyên dùng ổ khác ổ C):" Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,6" FontFamily="Segoe UI"/>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtDestPath" Grid.Column="0" Background="#121212" Foreground="#FFFFFF" BorderBrush="#3D3D3D" BorderThickness="1" Padding="8,6" FontSize="12" FontFamily="Segoe UI" VerticalAlignment="Center"/>
                                <Button x:Name="BtnBrowseDest" Grid.Column="2" Content="Chọn thư mục..." Style="{StaticResource SecondaryButtonStyle}" Click="BtnBrowseDest_Click" VerticalAlignment="Center"/>
                            </Grid>

                            <TextBlock x:Name="TxtDesktopStats" Text="Chưa quét dữ liệu Desktop." Foreground="#CCCCCC" FontSize="12" Margin="0,0,0,12" FontFamily="Segoe UI"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="BtnScanDesktop" Grid.Column="0" Content="Quét Desktop" Style="{StaticResource SecondaryButtonStyle}" Click="BtnScanDesktop_Click"/>
                                <Button x:Name="BtnStartBackup" Grid.Column="2" Content="Bắt đầu sao lưu" Style="{StaticResource PrimaryButtonStyle}" Click="BtnStartBackup_Click" IsEnabled="False"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource SectionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="2. XUẤT DANH SÁCH ỨNG DỤNG ĐÃ CÀI" Foreground="#00A2ED" FontSize="14" FontWeight="Bold" Margin="0,0,0,8" FontFamily="Segoe UI"/>
                            <TextBlock Text="Quét toàn bộ hệ thống và ghi ra file text danh sách các phần mềm bạn đã cài đặt trên máy này." Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,12" TextWrapping="Wrap" FontFamily="Segoe UI"/>
                            <Button x:Name="BtnExportApps" Content="Xuất danh sách ứng dụng (.txt)" Style="{StaticResource SecondaryButtonStyle}" Click="BtnExportApps_Click" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Nhật ký hoạt động (Terminal Log):" Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,6" FontFamily="Segoe UI"/>
                <TextBox x:Name="TxtLog" Grid.Row="1" Background="#181818" Foreground="#00FF00" BorderBrush="#2D2D2D" BorderThickness="1" FontFamily="Consolas" FontSize="11" IsReadOnly="True" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" TextWrapping="Wrap"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <TextBlock x:Name="TxtStatus" Text="Sẵn sàng" Foreground="#CCCCCC" FontSize="12" FontFamily="Segoe UI" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TxtPercentage" Text="0%" Foreground="#CCCCCC" FontSize="12" FontFamily="Segoe UI" HorizontalAlignment="Right"/>
            </Grid>
            <ProgressBar x:Name="ProgBar" Grid.Row="1" Height="4" Background="#1E1E1E" Foreground="#0078D4" Minimum="0" Maximum="100" BorderThickness="0"/>
        </Grid>
    </Grid>
</UserControl>
